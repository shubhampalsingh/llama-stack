# --- Database (optional — only powers the newsletter signups) ---
# Free Postgres from https://neon.tech. Without it, the site works fully;
# the subscribe form just returns an error.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
